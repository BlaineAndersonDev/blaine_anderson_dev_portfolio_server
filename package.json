{
  "name": "blaine-anderson-dev",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "scripts": {
      "start": "NODE_ENV=production node server.js",
      "cloudstart": "pm2 kill && pm2 start npm -- start && pm2 monit",
      "dev": "NODE_ENV=development nodemon server.js",
      "devreset": "dropdb rtd_dev && createdb rtd_dev && knex migrate:latest && knex seed:run && NODE_ENV=development nodemon server.js",
      "mocha": "NODE_ENV=test mocha --exit"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^3.21.2",
    "moment": "^2.24.0",
    "morgan": "^1.9.1",
    "node": "^13.1.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
